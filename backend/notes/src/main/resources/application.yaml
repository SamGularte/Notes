# =============================================================
# Define as variáveis de ambiente abaixo antes de rodar:
#
# DB_URL=jdbc:mysql://localhost:3306/secureNotes
# DB_USERNAME=root
# DB_PASSWORD=admin123
# JWT_SECRET=MXOP9wbTt01DP6bL1Q4evQjh2NG7Jgks9MmSFg2NA2J
# MAIL_USERNAME=securenotes18@gmail.com
# MAIL_PASSWORD=czpmfugikjczisgc
# GITHUB_CLIENT_ID=Ov23liOKBKwqL8KaYLTR
# GITHUB_CLIENT_SECRET=fd0425710e706bf30e59296397cae1b188802804
# FRONTEND_URL=http://localhost:3000
# =============================================================

spring:
  application:
    name: notes
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  app:
    jwtSecret: ${JWT_SECRET}
    jwtExpirationMs: 172800000
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  security:
    oauth2:
      client:
        registration:
          github:
            scope: read:user,user:email
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}

frontend:
  url: ${FRONTEND_URL}
